# Список TODO

## Дата последнего изменения

20.12.2025

## Задачи

- В запрос `/kits/{kit_id}` добавить возвращаемый параметр с типом набора.
- В запросе `/kits/{kit_id}` в возвращаемый параметр `box_contents` в количество добавить тип (1 уп., 2 шт. и т.д.) - возможно из номера перевести в строку.
- В запрос `/kits/{kit_id}` в возвращаемый параметр `analyses[]` добавить возвращаемый параметр с атриклем набора.
- В запрос `/kits/{kit_id}` возвращаемый параметр `video_url` заменить на `video_id` который соответствует `id` видео.
- В запросе `/analyses/{analysis_id}` в ответе должны дополнительно возвращаться следующие параметры:
  * тип материала (слюна и т.д.)
  * на макете имеется раздел - Исследование включает анализ генов: - надо как-то подвести под общую гребенку
  * подготовка - в виде массива строк
- ~Убрать метод `/labs` - вместо этого в метод `/kits/{kit_id}/analyses` к параметру `lab_name` добавить параметр `lab_id`.~
- ~~В запросе `/auth/token` - помимо `access_token` позвращать `id` набора (его тип/uuid).~~
- ~~Добавлять параметр цвета в запрос `/kits` или генерировать его случайным образом?~~ - **Генерируется случайно**

- В методе `/labs/{lab_id}/addresses` в массив `addresses` добавить время работы каждой лаборатории по конкретному адресу.

- Add protected routes
- Add QR Scanning
- Add code and auth verifying order details
- Add token to storage?
- State remembering