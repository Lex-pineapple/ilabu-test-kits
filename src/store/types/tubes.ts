export type RequiredTubesType = {
  tubes: TubeType[];
};

export type TubesCodesType = {
  codes: string[];
};

export type TubeType = {
  cap_color: string;
  quantity: number;
  tube_id: string;
  tube_name: string;
};
